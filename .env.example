# Reggia Docker CC — reads DEEPSEEK_API_KEY for auth
# The key is passed to Claude Code as ANTHROPIC_AUTH_TOKEN,
# with ANTHROPIC_BASE_URL pointed at DeepSeek's Anthropic-compatible endpoint.

# Get a key at https://platform.deepseek.com/api_keys
DEEPSEEK_API_KEY=sk-...
