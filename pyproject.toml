[project]
name = "reggia"
version = "0.1.0"
description = "Personal context orchestration layer — FastAPI backend wrapping Claude Code + Notion"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.34.0",
    "httpx>=0.28.0",
    "python-dotenv>=1.0.0",
]

[tool.uv]
dev-dependencies = []
